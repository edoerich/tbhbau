node_modules/
*.log
*.tmp
data/*.tmp
data/app.log
.DS_Store
.env

# snapshot vivo gerado pelo worker na VM (o versionado é o .seed.json)
public/data/snapshot.json
# série temporal por item gravada pelo worker na VM
public/data/history/

# saída do build estático (node build.mjs) — o Cloudflare Pages gera na hora do deploy
dist/
